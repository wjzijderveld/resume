<div class="skill">
    <h5>
        {% if skill.skill_url is defined %}<a href="{{ skill.skill_url }}">{% endif %}
            {{ skill.title }}
        {% if skill.skill_url is defined %}</a>{% endif %}
        &nbsp;-&nbsp;<span class="level">{{ skill.level }}</span></h5>
    <div class="description">{{ skill.description|paragraph }}</div>
</div>
